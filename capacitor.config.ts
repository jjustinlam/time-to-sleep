import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Time to Sleep',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
